export namespace CompositeTypes {
  export interface FileSystemComponent {
    getName(): string; // Returns the name of the component
    getSize(): number; // Returns the size of the component
  }
}
